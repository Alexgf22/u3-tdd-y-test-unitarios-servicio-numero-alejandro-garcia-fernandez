package es.edu.iesra.daw.agf.api

/*import kformat.*

class TextOutputFormatter : OutputFormatter {
    override fun format(registro: Registro): String {
        val builder = StringBuilder()
        builder.append("Resumen del juego:\n")
        registro.resumenCartones.forEach { (carton, lineas, bingo) ->
            builder.append("Cartón ${carton.id}:")
            if (lineas.isNotEmpty()) {
                builder.append(" Lineas: ${lineas.joinToString(", ")}")
            }
            if (bingo) {
                builder.append(" Bingo!")
            }
            builder.append("\n")
        }
        return builder.toString()
    }
}*/

