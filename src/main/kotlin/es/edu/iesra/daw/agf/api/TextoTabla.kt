package es.edu.iesra.daw.agf.api

/*import kformat.*

class TableOutputFormatter : OutputFormatter {
    override fun format(registro: Registro): String {
        val table = Table()
        table.addRow("Cartón", "Lineas", "Bingo")
        registro.resumenCartones.forEach { (carton, lineas, bingo) ->
            table.addRow(carton.id, lineas.joinToString(", "), if (bingo) "Sí" else "No")
        }
        return table.render()
    }
}*/